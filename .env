# Vulos OS Configuration — local development defaults.
#
# THIS FILE IS TRACKED IN GIT. Never put a secret in it.
# For local overrides (including anything secret) copy it to .env.local,
# which is gitignored and takes precedence when running --env=local.

PORT=8080
VULOS_DOMAIN=lvh.me
