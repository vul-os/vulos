{
  "name": "vulos-management-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The vulos-management authed SPA (sign-in + console). OSS build renders the NoOp commercial seam; the cloud build aliases @vulos/commercial-seam to its real implementation.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint ."
  },
  "dependencies": {
    "@fontsource-variable/geist-mono": "5.2.8",
    "@fontsource-variable/inter": "^5.2.8",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.1.0"
  }
}
