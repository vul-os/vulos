# docs/assets/screenshots

Generated by `npm run screenshots` (`scripts/screenshots/capture.mjs`).

Each image is the **real** server-rendered console page (the same
`pkg/superadmin` / `pkg/security` templates + CSS the control plane serves),
seeded with fabricated demo data by the Go dump harness — no operator data is
ever captured. Retina (1440-wide @2x). The console is dark-themed; the
security dashboard is light-themed. Neither themes dynamically.

| Image | Surface | Theme |
|-------|---------|-------|
| `dashboard.png` | Dashboard — Fleet billing cockpit — MRR, account counts, per-product cost rollup and the live audit feed. | dark |
| `accounts.png` | Accounts — Account search + admin — suspend, refund, force-reset, with every action audit-logged. | dark |
| `account-detail.png` | Account detail — A single account: verification / TOTP / session state and its billing transactions. | dark |
| `analytics.png` | Analytics — Users, DAU / MAU and per-product 7-day usage trends drawn as inline SVG sparklines. | dark |
| `orgs.png` | Orgs — Organisation directory — members, seats, tier and suspension state. | dark |
| `org-detail.png` | Org detail — One organisation: member roster, roles and a usage summary. | dark |
| `relay.png` | Relay & usage — Relay data-plane overview — PoP health per region and per-account GB / overage. | dark |
| `billing-recon.png` | Billing reconciliation — Revenue vs estimated COGS per tier, blended margin and drift-flagged tenants. | dark |
| `pricing.png` | Pricing — The SKU price catalogue — USD list price and the live ZAR the card is actually charged. | dark |
| `regions.png` | Regions — Per-region economics — egress cost, relay price, compute multiplier and real spend. | dark |
| `incidents.png` | Incidents — Status-page incidents and scheduled maintenance windows. | dark |
| `migrations.png` | Migrations — Aggregate schema state polled across every product in the suite. | dark |
| `auditlog.png` | Audit log — The tamper-evident, hash-chained audit log with actor / action filters. | dark |
| `reserved-handles.png` | Reserved handles — Reserved-username registry (postmaster, abuse, brand handles …). | dark |
| `maintenance.png` | Maintenance — Operator tools — verify the audit chain, check secret rotation. | dark |
| `login.png` | Operator login — The console sign-in: password + TOTP, then a WebAuthn hardware-key step-up. | dark |
| `security-dashboard.png` | Security dashboard — WAF hits, bot scores, step-up / ATO events, CT-log certs, egress anomalies and honeypot hits. | light |

Regenerate: `npm run screenshots`
