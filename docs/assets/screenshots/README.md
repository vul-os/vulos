# docs/assets/screenshots

Generated by `npm run screenshots` (`scripts/screenshots/capture.mjs`).

Each image is the **real** server-rendered console page (the same
`pkg/superadmin` / `pkg/security` templates + CSS the control plane serves),
seeded with fabricated demo data by the Go dump harness — no operator data is
ever captured. Retina (1440-wide @2x). The whole console — including the
security dashboard — ships a single deliberate dark instrument-panel theme.

| Image | Surface |
|-------|---------|
| `dashboard.png` | Operator console — The central cockpit — fleet health beacon, account / super-admin counts, open-incident state and the live hash-chained audit feed, with jump cards to every section. |
| `accounts.png` | Accounts — Account search + admin — suspend, force-reset and reset 2FA, with every action audit-logged. |
| `account-detail.png` | Account detail — A single account: verification / TOTP / session state, entitlements and recorded usage. |
| `analytics.png` | Analytics — Users, DAU / MAU and per-product 7-day usage trends drawn as inline SVG sparklines. Usage signals only. |
| `orgs.png` | Organisations — Organisation directory — members, seats, tier and suspension state. |
| `org-detail.png` | Org detail — One organisation: member roster, roles and a usage summary. |
| `relay.png` | Relay health — Relay data-plane health — per-region PoP status and throughput. Operational signals only. |
| `incidents.png` | Incidents — Status-page incidents and scheduled maintenance windows. |
| `migrations.png` | Migrations — Aggregate schema state polled across every product in the suite. |
| `auditlog.png` | Audit log — The tamper-evident, hash-chained audit log with actor / action filters. |
| `reserved-handles.png` | Reserved handles — Reserved-username registry (postmaster, abuse, brand handles …). |
| `maintenance.png` | Maintenance — Operator tools — verify the audit chain, check secret rotation. |
| `login.png` | Operator login — The console sign-in: password + TOTP, then a WebAuthn hardware-key step-up. |
| `security-dashboard.png` | Security dashboard — WAF hits, bot scores, step-up / ATO events, CT-log certs, egress anomalies and honeypot hits. |

Regenerate: `npm run screenshots`
