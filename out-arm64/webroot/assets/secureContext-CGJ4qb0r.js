function e(){let e=globalThis.crypto&&globalThis.crypto.subtle;if(e)return e;throw Object.assign(Error(`This feature needs browser encryption (WebCrypto) that only runs on a "secure" connection. This page was opened over plain http:// on an address browsers don't treat as secure (a LAN IP or a .local name), so it isn't available here — this is a browser restriction, not a problem with your data. Two ways to fix it: open the box over https:// (a self-signed certificate is enough — accept the browser's one-time warning and the connection becomes secure), or open an SSH tunnel to the box and use http://localhost:<port>, which browsers also treat as secure.`),{code:`INSECURE_CONTEXT`})}export{e as t};