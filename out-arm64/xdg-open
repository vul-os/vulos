#!/bin/sh
# xdg-open replacement for Vula OS — sends URLs to the frontend via notification
curl -sk -X POST https://localhost:8080/api/open \
  -H "Content-Type: application/json" \
  -H "X-Internal: true" \
  -d "{\"url\":\"$1\"}" >/dev/null 2>&1
