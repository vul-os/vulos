{{define "auditlog"}}
<h1>Audit Log</h1>
<form method="GET" action="/superadmin/auditlog" class="filter-bar">
  <input type="text" name="actor" value="{{.Actor}}" placeholder="actor">
  <input type="text" name="action" value="{{.Action}}" placeholder="action">
  <input type="text" name="from" value="{{.From}}" placeholder="from (RFC3339)">
  <input type="text" name="to" value="{{.To}}" placeholder="to (RFC3339)">
  <button type="submit" class="primary">Filter</button>
</form>
{{if .Rows}}
<div class="table-wrap">
<table>
<thead><tr><th>Seq</th><th>Time</th><th>Actor</th><th>Action</th><th>Target</th><th class="wrap">Metadata</th></tr></thead>
<tbody>
{{range .Rows}}
<tr>
  <td>{{.Seq}}</td>
  <td>{{.Ts}}</td>
  <td>{{.Actor}}</td>
  <td>{{.Action}}</td>
  <td>{{.Target}}</td>
  <td class="wrap">{{.MetadataJSON}}</td>
</tr>
{{end}}
</tbody>
</table>
</div>
{{else}}
<div class="empty">No entries found.</div>
{{end}}
{{end}}
